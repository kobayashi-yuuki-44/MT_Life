<div class="flex flex-col items-center justify-center h-screen space-y-4">
  <h1 class="text-2xl font-bold">分野選択</h1>
  <% @subjects.each do |subject| %>
    <%= link_to subject, show_subject_questions_path(subject: subject), class: "btn btn-primary" %>
  <% end %>
</div>
